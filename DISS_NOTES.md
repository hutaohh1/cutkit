# DISS_NOTES

## 评审对象

CutKit v2 架构总图 + `CutKit-v2-CAPABILITY-MAP.md` + `CutKit-ARCHITECTURE.md`。

## 开场暴击：当前方案的真实窟窿

1. **这是能力采购清单，不是可执行方案。**
   Skill、库、FFmpeg、Remotion、C2PA、OTIO 都被列出来了，但没有一个明确的垂直切片证明它们能组成可工作的系统。现在没有人能回答“第一个版本交付什么、谁用、验收是什么”。

2. **“确定性渲染”是目前最大的虚假承诺。**
   固定 FFmpeg/Chromium/Remotion 版本不等于可复现。GPU、CPU 指令、字体渲染、色彩管理、编码器线程、浏览器合成、VFR 素材和模型推理都会造成差异。方案没有定义语义一致、视觉近似、像素一致、字节一致四级复现，也没有定义哪一级是验收标准。

3. **编辑协议根本没有定义到能落地。**
   `base_revision + JSON Patch + 锁` 听起来很完整，但没有定义 split、merge、reorder、nested sequence、transition overlap、多轨音频、变速和外部编辑器回写时的 ID/区间语义。这个协议一遇到真实剪辑就会产生歧义。

4. **产品边界自相矛盾。**
   一边禁止 AI 生成视频、数字人和配乐，一边保留 Material Forge、生成式素材、provenance 和“生成内容披露”。允许什么、禁止什么、谁批准、如何证明没有冒充真实，没有一条可执行的规则。

5. **没有真正的用户闭环。**
   “用户 / Codex / 未来本地审阅界面 / 外部编辑器”不是角色模型。谁负责选择候选、谁负责审阅、谁可以批准、AI 能否修改、人工不同意时怎么办，全部悬空。

## 初步决策记录

- 暂不认可把公共 Skill 当成运行时组件。Skill 是方法和提示，不是带版本、签名、测试和故障语义的插件。
- 暂不认可“固定版本即可确定性”的说法。必须引入环境指纹和复现等级。
- 暂不认可 `master_timeline` 在外部编辑器回写后仍然天然唯一真相。必须定义 round-trip 所有权和冲突策略。
- 暂不认可 P0/P1/P2 是实施计划。它们只是能力分类，没有切片、依赖顺序、验收和回退策略。

## 当前最先要钉死的决策

**CutKit v2 第一个可交付版本到底是什么？**

候选：

1. **本地 CLI 垂直切片：摄取 → 简单组剪 → preview → 结构化 Review/Patch → final。**
   推荐。它能最快验证核心契约和闭环，且不依赖外部编辑器、C2PA、Forge 或复杂 Shader。
2. 外部编辑器适配器：`master_timeline` ↔ OTIO/FCPXML round-trip。
3. 能力注册表 + `doctor` + Render Plan。
4. 本地审阅 UI + 试片评审。

推荐：**选 1**。先证明“修改不会乱、渲染能重放、缺素材不会被伪造”；其余能力只能围绕这个核心接入。
